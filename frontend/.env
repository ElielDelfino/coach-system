# Copiar para .env e preencher os valores reais
# NUNCA commitar o .env

# URL da API — em produção (Swarm/nginx), use proxy reverso para o backend.
# Em dev local com `npm run dev`, o vite.config.js já proxia /api → http://localhost:3000.
VITE_API_URL=/api
